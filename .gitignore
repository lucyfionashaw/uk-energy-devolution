# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
.~lock.*#

# Keep the data/ JSON + workbook (they are the source of truth for data.js)

# Private framing essays — keep local, never publish to the public repo
docs/source-essays/
*.pdf

# Python bytecode
__pycache__/
*.pyc

# PlanIt per-term fetch cache (intermediate; raw combined file is committed)
data/raw/.planit_cache/
